name: collector

on:
  pull_request:
    branches: [v2, master]
    paths:
      - "collector/**"
      - "contracts/manifest/**"
      - "contracts/selection-policy.chrome-userdata-1.json"
      - ".github/workflows/collector.yml"
  push:
    branches: [v2, master]
    paths:
      - "collector/**"
      - "contracts/manifest/**"
      - "contracts/selection-policy.chrome-userdata-1.json"
      - ".github/workflows/collector.yml"

permissions:
  contents: read

jobs:
  test:
    name: collector / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: collector/go.mod
          cache-dependency-path: collector/go.mod
      - name: Test
        working-directory: collector
        run: go test ./...
      - name: Vet
        working-directory: collector
        run: go vet ./...

  race-and-cross-build:
    name: collector / race and six release targets
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: collector/go.mod
          cache-dependency-path: collector/go.mod
      - name: Race detector
        working-directory: collector
        run: go test -race ./...
      - name: Cross-build static binaries
        working-directory: collector
        shell: bash
        run: |
          set -euo pipefail
          for target in windows/amd64 windows/arm64 darwin/amd64 darwin/arm64 linux/amd64 linux/arm64; do
            os=${target%/*}
            arch=${target#*/}
            suffix=""
            if [[ "$os" == windows ]]; then suffix=".exe"; fi
            CGO_ENABLED=0 GOOS="$os" GOARCH="$arch" \
              go build -trimpath -o "/tmp/forensix-collect-$os-$arch$suffix" ./cmd/forensix-collect
          done
